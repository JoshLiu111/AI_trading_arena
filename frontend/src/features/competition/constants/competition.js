/**
 * Competition module constants
 */
export const COMPETITION_CONSTANTS = {
  // Timing delays (in milliseconds)
  AUTO_LOAD_DELAY: 100,
  STEP_DELAY: 100,
  STRATEGY_LOAD_DELAY: 1000,
  
  // Step numbers
  STEPS: {
    START: 1,
    STRATEGY: 2,
    TRADING: 3,
  },
};
